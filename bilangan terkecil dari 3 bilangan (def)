# 2 mencari bilangan terkecil dari tiga bilangan bulat A, B, dan C!
def kecil(a, b, c):
    if a < b and a < c:
        return a
    elif b < a and b < c:
        return b
    else:
        return c
A = int(input('masukkan bilangan pertama: '))
B = int(input('masukkan bilangan kedua: '))
C = int(input('masukkan bilangan ketiga: '))
print((kecil(A, B, C)))
